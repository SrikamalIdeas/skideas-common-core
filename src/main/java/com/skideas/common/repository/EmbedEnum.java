package com.skideas.common.repository;

/**
 * Enum for eager-loading hints for related entities.
 * Used by repositories to determine which relations to fetch eagerly.
 * Can be extended by features that need relation loading.
 */
public enum EmbedEnum {
    // Placeholder enum; features add specific values as needed.
}
